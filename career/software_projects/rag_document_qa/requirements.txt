# ---------------------------------------------------------------------------
# The CORE RAG pipeline, CLI, and tests require NOTHING here — they run on the
# Python standard library only. Everything below is OPTIONAL and only needed
# for the enhanced integrations described in the README.
# ---------------------------------------------------------------------------

# Optional: run the HTTP API (api.py)
#   pip install fastapi uvicorn
fastapi>=0.110
uvicorn>=0.29

# Optional: call real LLMs instead of the default MockLLM
#   pip install openai        (needs OPENAI_API_KEY)
#   pip install anthropic     (needs ANTHROPIC_API_KEY)
openai>=1.30
anthropic>=0.34

# Optional: swap the pure-Python TF-IDF retriever for dense embeddings
#   pip install sentence-transformers
sentence-transformers>=2.7
